package com.localstream.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette LocalStream, transposée du thème web Tailwind (bg-black / zinc / red-600).
 * Thème sombre unique — pas de variante claire pour l'instant.
 */

// Fond principal (bg-black).
val Black = Color(0xFF000000)

// Surfaces (zinc-900 / zinc-800).
val Zinc900 = Color(0xFF18181B)
val Zinc800 = Color(0xFF27272A)

// Texte et bordures.
val Zinc300 = Color(0xFFD4D4D8)
val Zinc500 = Color(0xFF71717A)
val White = Color(0xFFFFFFFF)

// Accent (red-600 / red-500).
val Red600 = Color(0xFFDC2626)
val Red500 = Color(0xFFEF4444)
